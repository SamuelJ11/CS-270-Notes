CHAPTER 6:  THE MEMORY HEIRARCHY

    • If you understand how data moves through the memory hierarchy, you can write your programs so that their data items are stored higher in the hierarchy, 
      where the CPU can access them more quickly.

      - this idea is based on a fundamental property of computer programs called locality
      
        -- programs with good locality tend to access the same data repeatedly or access data items that are located close to each other in memory

    I Storage Technologies

        A. Random Access Memory

            •  Random access memory (RAM) comes in two varieties — static and dynamic

                - SRAM is faster and much more expensive, used mainly for cache memory

                - DRAM is slower but cheaper, used for main memory and graphics buffers

                - most systems contain only a small amount of SRAM but much larger amounts of DRAM

            • SRAM stores each bit in a bistable memory cell.

                - imagine a pendulum that can fall left or right; if it’s all the way to the left (bit = 0) or right (bit = 1) it's stable

                - if it's perfectly upright, its a metastable balance point (think neither 0 nor 1 yet)

                - if you slightly disturb the pendulum when it’s balanced upright, it will quickly fall to one side — and then stay there:
                    -- that’s just like the SRAM cell: if the voltage somehow gets disturbed and both sides are nearly equal, it’ll quickly “decide” 
                       which stable configuration to fall into — 0 or 1

            • DRAM stores each bit as charge on a capacitor. This capacitor is very small, typically around 30 femtofarads (30 × 10^-15) farads. 

                - unlike SRAM, a DRAM memory cell is very sensitive to any disturbance
                
                - when the capacitor voltage is disturbed, it will never recover
            
            • Various sources of leakage current cause a DRAM cell to lose its charge within a time period of around 10 to 100 milliseconds

                - since clock cycle times are measured in nano-seconds, this is actually quite a long time

                - the memory system must periodically refresh every bit of memory by reading it out and then rewriting it

            • The cells (bits) in a DRAM chip are partitioned into 'd' supercells, each consisting of 'w' DRAM cells

                - A d × w DRAM stores a total of dw bits of information
                
                - each supercell (d) is organized as a rectangular array with r rows and c columns (rc = d), and has an address of the form (i, j), 
                  where i denotes the row and j denotes the column

            • See figure 6.3 on page 611 for a high-level overview of a 16 × 8 DRAM chip

                -  d = 16 supercells, w = 8 bits per supercell, r = 4 rows, and c = 4 columns

                - information flows in and out of the chip via 1-bit external connectors called pins:

                    -- eight data pins transfer 1 byte of data

                    -- two address pins carry row and column supercell addresses

                -  Each DRAM chip is connected to the memory controller (via the pins above) which can transfer w bits at a time to and from each DRAM chip

                - to read the contents of a supercell (i, j):
                    
                    -- first the memory controller sends the row address (i) to the DRAM, followed by the column address (j)

                    -- second, the DRAM responds by sending the contents of supercell (i, j) back to the controller

                - the row address is called the Row Access Strobe (RAS) request, and we use CAS (Column Access Strobe) request for the column address

                    -- example: supercell (2, 1) is read by first having the memory controller (MC) copy the entire contents of row 2 into an internal row buffer,
                                then the MC sends column address 1, to which the DRAM responds by copying the 8-bit supercell from the row buffer to the MC

            • Each DRAM chip can only handle a small number of bits at once — for example, 8 bits (1 byte) per transfer, but the CPU works with much wider data paths of
              64 bits (8 bytes) at a time.

            • Since a single DRAM chip isn’t enough to feed the CPU efficiently, manufacturers group several DRAM chips together on a memory module (like a stick of RAM).

                - those chips work in parallel, so their individual 8-bit data outputs combine into a 64-bit-wide bus

            • See figure 6.5 on page 613 to see how DRAM cells are organized in parallel to output 64-bit words to main memory:

                - each supercell stores 1 byte of main memory, and each 64-bit word at byte address 'A' in main memory is represented by the eight supercells
                  whose corresponding supercell address is (i, j)

                - to retrieve the word at memory address A, the memory controller converts A to a supercell address (i, j) and sends it to the memory module, 
                  which then broadcasts i and j to each DRAM
                  
                - in response, each DRAM outputs the 8-bit contents of its (i, j) supercell

                -  Circuitry in the module collects these outputs and forms them into a 64-bit word, which it returns to the memory controller

            • When the CPU needs to read or write data at some address A, the memory controller has to figure out which memory module actually stores this address

                - main memory is divided across several modules that hold a different range of addresses
                
                - the memory controller looks at address A and determines which module (k) it belongs to

            • There are many kinds of DRAM memories, and various optimizations have been made to speed up thier DRAM cell access time:

                - Fast page mode DRAM (FPM DRAM): speeds up memory access by taking advantage of multiple reads from the same row

                    -- once a row is opened and copied into the row buffer, subsequent accesses to that same row only need CAS signals

                - Extended data out DRAM (EDO DRAM): An enhanced form of FPM DRAM that allows the individual CAS signals to be spaced closer together in time

                ** These older types of DRAM (like Fast Page Mode or Extended Data Out) are asynchronous, meaning they don't use a shared, continuous clock signal to time their operations

                - Synchronous DRAM (SDRAM): uses the same external clock signal that is already driving the memory controller, therefore it can output the contents 
                  of its supercells at a faster rate than its asynchronous counterparts

                - Double Data-Rate Synchronous DRAM (DDR SDRAM):  an enhancement of SDRAM that doubles the speed of the DRAM by using both clock edges as control signals

                    -- the generational upgrades (DDR2, DDR3, etc.) use a feature called the prefetch buffer (a small, high-speed staging area on the DRAM chip itself)
                       to increase the effective bandwidth

                - Video RAM (VRAM): designed specifically engineered to handle the continuous and simultaneous data demands of a display (frame buffer for a graphics system)

            • DRAM and SRAM are volatile in the sense that they lose their information if the supply voltage is turned off.

                - non-volatile memories are known as read-only memories (ROMs) for historical reasons, although some types can be written to and read from

            • ROMs are distinguished by the number of times they can be reprogrammed (written to) and by the mechanism for reprogramming them:

                - a programmable ROM (PROM) can be programmed exactly once, and include a sort of fuse with each memory cell that can be blown once by zapping
                  it with a high current

                - an erasable prgrammable ROM (EPROM) has a transparent quartz window that permits light to reach the storage cells

                    -- ALL cells are cleared to zeroes (global erase) by shining ultraviolet light through the window
                    -- a special device is used to write ones into the EPROM, and can be reprogrammed on the order of 1000 times
                    -- electictally erasable PROMs (EEPROMs) can erase and rewrite individual bytes using opposite-polarity electrical signals, and
                       the chip does not need to be removed from the circuit board

                - flash memories are a type of nonvolatile memory, based on EEPROMs - used in SSD's, digital cameras, cell phones, etc.

            • Programs stored in ROM devices are often referred to as 'firmware':

                - when a computer system is powered up, it runs firmware stored in a ROM (ex: PC's BIOS)

            • Data flows back and forth between the processor and the DRAM main memory over shared electrical conduits called 'buses'

                - a read transaction transfers data from the main memory to the CPU

                - a write transaction transfers data from the CPU to the main memory

            • A bus is a collection of parallel wires that carry address, data and control signals

            • See figure 6.6 on page 616 for the following description:

                - the CPU chip contains the ALU, register file, and bus interfeace
                
                - the I/O bridge contains the memory controller, and the DRAM memory modules make up the main memory

                - the system bus connects the CPU to the I/O bridge, and the memory bus connects the I/O bridge to main memory
            
            • Consider what happens when the CPU performs a load operation 'mov A, %rax':    

                - step 1: Address Request (CPU → Memory)

                    -- the CPU's bus interface places the memory address A onto the system bus
                    -- the I/O Bridge (or Northbridge/Memory Controller Hub) relays the address from the system bus to the memory bus

                - step 2: Data Retrieval (Memory → Memory Bus)

                    -- the Main Memory (DRAM) senses the address on the memory bus.
                    -- it fetches the requested data word (the contents of address A) and places this data onto the memory bus
                    -- the I/O Bridge translates the data signal and passes it to the system bus.

                - step 3: Data Load (System Bus → CPU Register)

                    -- the CPU senses the data on the system bus
                    -- it reads the data and copies it into the destination register, %rax

        B. Disk Storage

            • Disks are workhorse storage devices that hold enormous amounts of data, on the order of hundreds to thousands of gigabytes, as opposed to the 
              hundreds or thousands of megabytes in a RAM-based memory.

            • Disks are constructed from platters, and each platter consists of two sides (surfaces) that are coated with magnetic recording material.

                - a rotating spindle in the center of the platter spins the platter at a fixed rotational rate, typically between 5,400 and 15,000 revolutions 
                  per minute (RPM)

                  -- a CD is basically a single platter
                  -- A hard drive is a stack of multiple platters, each spinning on the same spindle, which allows it to store much more data than a single CD

            • Each surface consists of a collection of concentric rings called 'tracks'.

                - each track is partitioned into a collection of sectors, and each sector contains an equal number of data bits (typically
                  512 bytes) encoded in the magnetic material

                - sectors are separated by 'gaps' where no data bits are stored, and serve as formatting bits that identify sectors

            • A disk consists of one or more platters stacked on top of each other and encased in a sealed package (called a disk drive or just 'disk')

            • A cylinder is the collection of tracks on all the surfaces that are equidistant from the center of the spindle  

            • The maximum number o fbits that can be recorded by a disk is known as its maximum capacity, determined by:

                - recording density: the number of bits that can be squeezed into a 1-inch segment of a track

                - track density: the number of tracks that can be squeezed into a 1-inch segment of the radius extending from the center of the 
                                 platter

                - areal density: the product of the recording and track density (bits/in^2)

            • Areal density is doubling every couple years

                - original disks partitioned every track into the same number of sectors determined by the number of sectors that could
                  be recorded on the inner-most (smallest) track

                    -- as areal densities increased, gaps became unacceptably large, so modern high-capacity disks use a technique known as 
                       'multiple zone recording' where the set of cylinders is partitioned into disjoint subsets known as 
                        recording zones (rings of cylinders)

                    -- each zone consists of a contiguous collection of cylinders (cylinders that are next to each other radially)                    
                  
                    -- every track in all cylinders of that zone has the same number of sectors

                    -- outer zones have longer tracks → more sectors

                    -- inner zones have shorter tracks → fewer sectors
                    
            • Capacity = (# of bytes per sector) * (average # of sectors per track) * (# of tracks per surface) * (# of surfaces per platter) * (# of platters)

            • Disks read and write bits stored on their magnetic surfaces using a read/write head connected to the end of an actuator arm (see figure 6.10 on 
              page 621)

                - the mechanical motion of moving the arm back and forth along its raida axix above any track on the surface is known as a 'seek'

                    -- once the head is positioned over the the desired track, as each bit on the track passes underneath, the head can can sense the 
                       value of the bit (read) or alter the value of the bit (write)

                - at any point in time, all heads are lined up vertically and move in unison (positioned on the same cylinder)

                - the read/write head at the end of the arm flies on a thin cushion of air over the disk surface at a height of about 0.1 microns and a speed of about 80 km/h 
                - this is analogous to placing a skyscraper on its side and flying it around the world at a height of 2.5 cm (1 inch) above the ground, with each orbit taking only 8 seconds 
                - at these tolerances, even a tiny piece of dust on the surface is like a huge boulder. If the head were to strike one of these boulders, it would stop flying and crash into the surface, 
                  resulting in a so-called head crash 
                - for this reason, disks are always sealed in airtight packages

            • Disks read and write data in sector-size blocks and the access time for a sector has three main components: 
            
                - seek time: the time required to move the arm, on average is 3 - 9 milliseconds

                - rotational latency: the delay while the disk spins to position the target sector under the read/write head after it reaches the correct track 
                                      the worst-case latency occurs if the head just misses the sector and must wait for a full rotation

                                      maximum rotational latency = (1 / RPM) * (60 sec / 1 min)
                                      average rotational latency = 1/2 of maximum rotational latency

                - transfer time: the transfer time for one sector depends on the rotational speed and the number of sectors per track:

                    average transfer time = (1 / RPM) * (1 / average # of sectors/track) * (60 secs / 1 min)

            • We can estimate the average time to access the contents of a disk sector (access time) as the sum of the average seek time, the average rotational latency, 
              and the average transfer time (see page 622 for an example calculation)

                - The access time for a 64-bit word stored in SRAM is roughly 4 ns, and 60 ns for DRAM:

                    -- access time for a 512-byte word in SRAM is thus 4 x 64 = 256 ns
                    -- access time for a 512-byte word in DRAM is thus 60 x 64 = 4,000 ns

                - The disk access time, roughly 10 ms, is about 40,000 times greater than SRAM, and about 2,500 times greater than DRAM!

            • Modern disks are complex; disks have multiple platters, multiple surfaces, and different zones on those surfaces.

                - to hide this complexity fromthe operating system, modern disks present a simpler view of their geometry as
                  a sequence of B sector-size logical blocks, numbered 0, 1,...,B− 1

                - a small hardware/firmware device in the disk package, called the 'disk controller', maintains the mapping between logical block numbers 
                  and actual (physical) disk sectors

                - when the operating system wants to perform an I/O operation such as reading a disk sector into main memory, it sends a command to the 
                  disk controller asking it to read a particular logical block number

                  -- firmware on the controller performs a fast table lookup that translates the logical block number into a (surface, track,
                     sector) triple that uniquely identifies the corresponding physical sector

                  -- hardware on the controller interprets this triple to move the heads to the appropriate cylinder, waits for the sector to pass 
                     under the head, gathers up the bits sensed by the head into a small memory buffer on the controller, and copies them into main memory

            • Input/output (I/O) devices such as graphics cards, monitors, mice, keyboards, and disks are connected to the CPU and main memory using an I/O bus.

            • Although the I/O bus is slower than the system and memory buses, it can accommodate a wide variety of third-party I/O devices:

                - a Universal Serial Bus (USB) controller is a conduit for devices attached to a USB bus

                - a graphics card (or adapter) contains hardware and software logic that is responsible for painting the pixels on the display monitor 
                  on behalf of the CPU

                - a host bus adapter connects one or more disks to the I/O bus using a specific communication protocol, with the two most common interfaces being SCSI and SATA 
                  
                    -- SCSI disks are generally faster and more expensive than SATA drives; a SCSI adapter can support multiple disks, whereas a SATA adapter supports only one

                - Additional devices, such as network adapters, can be attached to the I/O bus by plugging them into empty expansion slots on the motherboard that provide a direct 
                  electrical connection to the I/O bus

            • See figure 6.12 on page 627 for a summary of the steps that take place when a CPU reads data from a disk:

                - the CPU issues commands to I/O devices using a technique called 'memory-mapped I/O'; in a system with memory-mapped I/O, a block of addresses in the address space 
                  is reserved for communicating with I/O devices

                - each of these addresses is known as an I/O port, and each device is associated with one or more ports when attached to the bus

                - As a simple example, suppose that the disk controller is mapped to port 0xa0:

                (1) CPU initiates a disk read by executing three store instructions to address 0xa0:

                    - first instruction sends a read command instruction + other parameters like interrupt CPU when done
                    - second instruction indicates the logical block number that should be read
                    - third instruction indicates the main memory address where the contents of the disk sector should be stored

                (2) after the disk controller recieves the read command from the CPU, is translates the logical block number to a sector 
                    address, reads the contents of the sector, and tranfers the contents directly to main memory, without any intervention
                    form the CPU

                    - This is known as a direct memory access (DMA) and the transfer of this data is a DMA transfer

                (3) after the DMA transfer is complete and the contents are stored in main memory, the disk controller notifies the CPU by
                    sending an interrupt signal (signals an external pin on the CPU chip)

                    - this causes the CPU to stop what it is doing and jump to an operating system routine, which records the fact that
                      the I/O has finished and then returns control to the point where the CPU was interrupted

        C. Solid State Disks

                